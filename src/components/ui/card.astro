---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div
  class={`rounded-2xl border border-black/10 bg-white p-8 ${className}`.trim()}
>
  <slot />
</div>
